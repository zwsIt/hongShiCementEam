package com.supcon.mes.module_warn.ui.view;

import android.content.Context;

import com.supcon.mes.mbap.view.CustomDialog;

public class CustomDelayDialog extends CustomDialog {
    public CustomDelayDialog(Context context) {
        super(context);
    }

    public CustomDelayDialog(Context context, int themeResId) {
        super(context, themeResId);
    }

    @Override
    public CustomDialog layout(int resId) {
        return super.layout(resId);
    }
}
